dummy text

This webapp will allow users to track the weather along a driving route

TO be continued.....


To run the app, first download it and terminal into the root directory using 

cd weathertrip

from there, open a new terminal window and move to the server directory

cd server

install dependencies with

npm install

and start the server

npm start


in the root directory, do the same

npm install
npm start

you should then be able to access the app on localhost:3000